# Local mode works without credentials when bound to loopback.
APP_PROFILE=local
DASHBOARD_PASSPHRASE=

# Required with APP_PROFILE=server; optional locally for cursors to survive restarts.
# Generate independently from the dashboard passphrase: openssl rand -base64 32
DOCUMENT_CURSOR_SECRET=
